#include "Tests.h"
#include "Controller.h"
#include <assert.h>

using namespace std;

void Tests::TestAll() {
	/*
	Repository repo{"Tests.txt"};
	assert(repo.getTasks().size() == 2);
	assert(repo.getTasks()[0].getDescr() == "study");
	repo.deleteTask(Task{ "study", "open", -1 });
	assert(repo.getTasks().size() == 1);
	assert(repo.getTasks()[0].getDescr() == "learn");
	Controller ctrl{ repo };
	ctrl.deleteTask("learn");
	assert(ctrl.getRepo().getTasks().size() == 0);
	ctrl.addTask("sleep");
	Programmer p{ "maria", 4 };
	ctrl.startTask("sleep", p);
	assert(ctrl.getRepo().getTasks()[0].getStatus() == "in progress");
	assert(ctrl.getRepo().getTasks()[0].getIdp() == 4);*/
}